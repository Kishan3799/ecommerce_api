[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
  "python manage.py migrate"
]

[start]
cmd = "gunicorn ecommerce_api.wsgi:application --bind 0.0.0.0:8080 --workers 2 --timeout 120 --log-level debug"